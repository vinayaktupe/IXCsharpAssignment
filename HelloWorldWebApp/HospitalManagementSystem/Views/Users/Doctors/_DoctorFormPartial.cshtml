@using role = HospitalManagementSystem.Data.Model.Role
@model HospitalManagementSystem.ViewModel.UserDoctorDetailsViewModel


<div id="DoctorForm">
    @*<partial name="~/Views/Users/Doctors/_CreateDoctorPartial.cshtml" />*@
    <div class="form-group">
        <label asp-for="Doctor.Specialization" class="control-label"></label>
        <input asp-for="Doctor.Specialization" class="form-control" />
        <span asp-validation-for="Doctor.Specialization" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Doctor.YOE" class="control-label"></label>
        <input asp-for="Doctor.YOE" class="form-control" />
        <span asp-validation-for="Doctor.YOE" class="text-danger"></span>
    </div>
</div>

@section Scripts {
    @*<script>

        document.readyState = () => {
            const doctorForm = document.querySelector("#DoctorForm");
            const selectRole = document.querySelector("#SelectRole");
            if (doctorForm) {
                doctorForm.style.display = "none";
                selectRole.addEventListener("change", (event) => {
                    if (event.target.value == @Convert.ToInt32(role.Doctor)) {
                        doctorForm.style.display = "block";

                    } else {
                        doctorForm.style.display = "none";

                    }
                });
            }
        };

    </script>*@
}
@*$(document).ready(() => {
        $("#DoctorForm").hide();
        $("#SelectRole").change((event) => {
            if (event.target.value == @Convert.ToInt32(role.Doctor)) {
                $("#DoctorForm").show();
            } else {
                $("#DoctorForm").hide();
            }
        });
    });*@