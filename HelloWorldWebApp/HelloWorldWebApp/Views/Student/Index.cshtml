@{
    ViewData["Title"] = "Student | Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using HelloWorldWebApp.Models;

@{
    dynamic students = ViewData["students"];
}

<div class="row mt-4">
    <div class="col-10 offset-1">
        <table class="table table-bordered table-hover table-responsive table-striped">
            <thead class="text-center bg-dark text-light">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Year</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (Student student in students)
                    {
                        string strYear = @student.Year == 1 ? "First Year" : @student.Year == 2 ? "Second Year" : @student.Year == 3 ? "Third Year" : "Final Year";
                        <tr class="text-center">
                            <th>@student.ID</th>
                            <th>@student.Name</th>
                            <th>@strYear</th>
                        </tr>
                    }
                }
            </tbody>
            <tfoot class="text-center bg-dark text-light">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Year</th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>
